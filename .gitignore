target/
/.zeroclaw-ref
**/*.rs.bk
*.pdb

# Never commit a keypair, including the attestation issuer.
*.json.key
id.json
issuer*.json
.env

# Nor a provider token. This repo gets published; the bounty requires
# secrets redacted, and a leaked token here is a real compromise.
*.token
token.txt
auth.json
credentials.json
*.pem
.issuer.json
.sas-ref
